import BottomInputBar from "@/components/BottomInputBar";

export default function Home() {
  return (
    <main className="flex relative h-[calc(100svh-56px)] w-full max-w-3xl  mx-auto flex-col items-center justify-between">
      Hello
      <BottomInputBar />
    </main>
  );
}
